# e18-custom-promisified-http-request

> simple *promisified* library to submit-http-requests

## Description

A very simple promise-based library to submit GET http requests.

It supports:
+ both HTTP and HTTPS requests
+ basic retryable logic
