# 04-another-webapp-with-ejs
> Another webapp returning HTML using `ejs` as the template engine

## Description

A simple HTTP server that returns HTML using the `ejs` template engine. Nothing groundbreaking here.