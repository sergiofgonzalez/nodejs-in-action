/* client-side TypeScript here */

// eslint-disable-next-line @typescript-eslint/no-inferrable-types
const msg: string = `Everything's perfectly all right now. We're fine. We're all fine here now, thank you. How are you?`;

console.log(msg);
