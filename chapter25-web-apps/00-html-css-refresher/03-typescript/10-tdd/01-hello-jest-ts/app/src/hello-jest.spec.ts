
/* this test will fail */
test('should be false and thus will fail', () => {
  expect(false).toBeFalsy();
});

/* this test will pass */
test('should be false and thus will pass', () => {
  expect(false).toBeFalsy();
});