import { getFlights } from '../src/lib/flights';

// const flights = getFlights();

describe.skip('bookings tests', () => {

  test('create a booking', () => {
    expect(true).toBeFalsy();
  });

  test('pay for a booking', () => {
    expect(true).toBeFalsy();
  });

  test('complete a booking', () => {
    expect(true).toBeFalsy();
  });
});
